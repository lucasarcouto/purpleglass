# Port Configuration (for reference, actual port mapping handled by docker-compose)
FRONTEND_PORT=3000
BACKEND_PORT=5001

# Backend API URL - Vite doesn't support variable substitution, so hardcode the URL
VITE_BACKEND_URL=http://localhost:5001
