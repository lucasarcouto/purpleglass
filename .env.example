# Docker Compose Configuration
# This file contains variables used by docker-compose.yml for container orchestration

# Port Configuration
FRONTEND_PORT=3000              # Port exposed to host for frontend
BACKEND_PORT=5001               # Port exposed to host for backend API
BACKEND_CONTAINER_PORT=5000     # Port inside backend container (should match backend PORT)

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=purpleglass
POSTGRES_PORT=5432              # Port exposed to host for PostgreSQL

# Note: Application-specific variables are in backend/.env and frontend/.env
# This file is only for docker-compose infrastructure configuration
