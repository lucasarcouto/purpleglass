NODE_ENV=development

# Backend Server Configuration
PORT=5000                      # Port the backend server listens on internally

# CORS Configuration (should match frontend URL)
CORS_ORIGIN=http://localhost:3000

# Database Connection URL
# For Docker: Use 'postgres' as hostname (Docker service name)
# For local development: Use 'localhost' as hostname
# Note: POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_PORT are defined in root .env
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/purpleglass?schema=public"

# JWT Authentication - CHANGE THIS TO A SECURE RANDOM STRING
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET="your_secure_random_string_here"

# Vercel Blob Storage - REQUIRED FOR FILE UPLOADS
# Get your token from: https://vercel.com/docs/storage/vercel-blob/quickstart
BLOB_READ_WRITE_TOKEN="your_vercel_blob_token_here"

# Note: FFmpeg is required for audio transcoding
# Install via: brew install ffmpeg (macOS), apt-get install ffmpeg (Ubuntu), choco install ffmpeg (Windows)